<Window x:Class="TempRamDiskGUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        ui:WindowHelper.UseModernWindowStyle="True"
        Title="TEMP RAM Disk Manager" 
        Height="700" Width="1000"
        MinHeight="600" MinWidth="800"
        Icon="star.ico"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <!-- Custom Styles -->
        <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#2E86AB"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>
        
        <Style x:Key="SubHeaderTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#4A4A4A"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
        </Style>
        
        <Style x:Key="InfoTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="#6A6A6A"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>

        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="40"/>
            <Setter Property="MinWidth" Value="120"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <Style x:Key="CreateButtonStyle" TargetType="Button" BasedOn="{StaticResource ActionButtonStyle}">
            <Setter Property="Background" Value="#28A745"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#28A745"/>
        </Style>

        <Style x:Key="RemoveButtonStyle" TargetType="Button" BasedOn="{StaticResource ActionButtonStyle}">
            <Setter Property="Background" Value="#DC3545"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#DC3545"/>
        </Style>

        <Style x:Key="RefreshButtonStyle" TargetType="Button" BasedOn="{StaticResource ActionButtonStyle}">
            <Setter Property="Background" Value="#007BFF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#007BFF"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header Section -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Image Grid.Column="0" Source="star.ico" Width="48" Height="48" Margin="0,0,15,0"/>
            
            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                <TextBlock Style="{StaticResource HeaderTextStyle}">TEMP RAM Disk Manager</TextBlock>
                <TextBlock Style="{StaticResource InfoTextStyle}">
                    Create virtual disks from your computer's RAM for ultra-fast storage. Perfect for speeding up games, development, and temporary files.
                </TextBlock>
            </StackPanel>

            <StackPanel Grid.Column="2" VerticalAlignment="Center">
                <TextBlock Text="{Binding SystemMemoryInfo}" Style="{StaticResource InfoTextStyle}" TextAlignment="Right"/>
                <TextBlock Text="{Binding DriverStatus}" Style="{StaticResource InfoTextStyle}" TextAlignment="Right"/>
            </StackPanel>
        </Grid>

        <!-- Control Panel -->
        <Border Grid.Row="1" Background="#F8F9FA" CornerRadius="8" Padding="20" Margin="0,0,0,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Create RAM Disk Panel -->
                <StackPanel Grid.Column="0">
                    <TextBlock Style="{StaticResource SubHeaderTextStyle}">Create New RAM Disk</TextBlock>
                    
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="60"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="10"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="10"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Size -->
                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Size:" VerticalAlignment="Center" FontWeight="SemiBold"/>
                        <TextBox Grid.Row="0" Grid.Column="1" x:Name="SizeTextBox" Text="256" VerticalAlignment="Center" Height="30"/>
                        <ComboBox Grid.Row="0" Grid.Column="3" x:Name="SizeUnitComboBox" SelectedIndex="1" VerticalAlignment="Center" Height="30">
                            <ComboBoxItem Content="KB"/>
                            <ComboBoxItem Content="MB"/>
                            <ComboBoxItem Content="GB"/>
                        </ComboBox>

                        <!-- Drive Letter -->
                        <TextBlock Grid.Row="0" Grid.Column="5" Text="Drive:" VerticalAlignment="Center" FontWeight="SemiBold"/>
                        <ComboBox Grid.Row="0" Grid.Column="7" x:Name="DriveLetterComboBox" VerticalAlignment="Center" Height="30"/>

                        <!-- Options -->
                        <CheckBox Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" x:Name="RemovableCheckBox" Content="Removable Media" VerticalAlignment="Center"/>
                        <CheckBox Grid.Row="2" Grid.Column="3" Grid.ColumnSpan="2" x:Name="CdRomCheckBox" Content="CD-ROM Type" VerticalAlignment="Center"/>

                        <!-- Create Button -->
                        <Button Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="8" x:Name="CreateButton" 
                                Style="{StaticResource CreateButtonStyle}" Click="CreateButton_Click"
                                HorizontalAlignment="Left" Width="200">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="⭐" FontSize="16" Margin="0,0,5,0"/>
                                <TextBlock Text="Create RAM Disk"/>
                            </StackPanel>
                        </Button>
                    </Grid>
                </StackPanel>

                <!-- Quick Actions Panel -->
                <StackPanel Grid.Column="2">
                    <TextBlock Style="{StaticResource SubHeaderTextStyle}">Quick Actions</TextBlock>
                    
                    <Button x:Name="RefreshButton" Style="{StaticResource RefreshButtonStyle}" Click="RefreshButton_Click" Margin="0,5">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🔄" FontSize="14" Margin="0,0,5,0"/>
                            <TextBlock Text="Refresh"/>
                        </StackPanel>
                    </Button>
                    
                    <Button x:Name="RemoveAllButton" Style="{StaticResource RemoveButtonStyle}" Click="RemoveAllButton_Click" Margin="0,5">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🗑️" FontSize="14" Margin="0,0,5,0"/>
                            <TextBlock Text="Remove All"/>
                        </StackPanel>
                    </Button>
                    
                    <Button x:Name="AboutButton" Style="{StaticResource ActionButtonStyle}" Click="AboutButton_Click" Margin="0,5">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="ℹ️" FontSize="14" Margin="0,0,5,0"/>
                            <TextBlock Text="About"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- RAM Disks List -->
        <Border Grid.Row="2" Background="White" CornerRadius="8" BorderBrush="#DEE2E6" BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- List Header -->
                <Border Grid.Row="0" Background="#E9ECEF" CornerRadius="8,8,0,0" Padding="15,10">
                    <TextBlock Style="{StaticResource SubHeaderTextStyle}" Margin="0">Active RAM Disks</TextBlock>
                </Border>

                <!-- DataGrid -->
                <DataGrid Grid.Row="1" x:Name="RamDisksDataGrid" 
                          AutoGenerateColumns="False" 
                          CanUserAddRows="False" 
                          CanUserDeleteRows="False"
                          CanUserReorderColumns="False"
                          CanUserResizeRows="False"
                          HeadersVisibility="Column"
                          GridLinesVisibility="Horizontal"
                          AlternatingRowBackground="#F8F9FA"
                          SelectionMode="Single"
                          RowHeight="40"
                          FontSize="13"
                          Margin="0">

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Device" Binding="{Binding DeviceNumber}" Width="80" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Drive" Binding="{Binding DriveLetter}" Width="60" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Size" Binding="{Binding SizeFormatted}" Width="100" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Type" Binding="{Binding DeviceType}" Width="120" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Memory Used" Binding="{Binding MemoryUsedFormatted}" Width="120" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Hit Ratio" Binding="{Binding CacheHitRatio}" Width="100" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Read/Write" Binding="{Binding ReadWriteInfo}" Width="150" IsReadOnly="True"/>
                        <DataGridTemplateColumn Header="Actions" Width="*" CanUserResize="False">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <Button Content="📊 Stats" Style="{StaticResource ActionButtonStyle}" 
                                                Click="StatsButton_Click" Tag="{Binding DeviceNumber}"
                                                Height="30" MinWidth="80" FontSize="11" Margin="2"/>
                                        <Button Content="📂 Open" Style="{StaticResource ActionButtonStyle}" 
                                                Click="OpenButton_Click" Tag="{Binding DriveLetter}"
                                                Height="30" MinWidth="80" FontSize="11" Margin="2"/>
                                        <Button Content="🗑️ Remove" Style="{StaticResource RemoveButtonStyle}" 
                                                Click="RemoveButton_Click" Tag="{Binding DeviceNumber}"
                                                Height="30" MinWidth="80" FontSize="11" Margin="2"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <!-- Status Bar -->
        <Border Grid.Row="3" Background="#E9ECEF" CornerRadius="4" Padding="10,5" Margin="0,10,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Grid.Column="0" x:Name="StatusText" Text="Ready" VerticalAlignment="Center" FontSize="12"/>
                <TextBlock Grid.Column="1" x:Name="VersionText" Text="TEMP v1.0.0" VerticalAlignment="Center" FontSize="12" Foreground="#6A6A6A"/>
            </Grid>
        </Border>
    </Grid>
</Window> 